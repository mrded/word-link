const expect = require('chai').expect;
const WordLink = require('../');

module.exports = function() {
  describe('Given html text', function() {
    it('Replace one word');
    it('Replace one word with regex');
    it('Replace two words with regex');

    it('Replace words and ignore <a> tags');
  });
}
